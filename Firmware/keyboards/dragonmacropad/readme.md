# Firmware

**QMK**: This is a QMK firmware port of the dragonmacropad.

The firmware includes support for the 3×3 key matrix, rotary encoder, RGB LEDs and OLED display.

The compiled `.uf2` file is also included and ready to flash.